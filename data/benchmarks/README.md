# Benchmark Data

This directory anchors local benchmark datasets used by the public-memory benchmark runners.

Large or licensed dataset JSON files under `locomo/`, `longmemeval/`, and `beam/` are intentionally ignored by Git. Supply them explicitly before running the dataset-backed benchmark commands; release artifacts must record the exact manifest, hashes, and raw outputs used for any published claim.
