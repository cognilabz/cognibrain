# Memory Operating Protocol First

Cognibrain v1 will ship the repo-installed Memory Operating Protocol as the primary artifact, with the CLI and runtime supporting that agent behavior rather than defining the product on their own. This keeps the market promise focused on agents that remember, judge, guard, store, and prove work through an installed harness/skill/plugin instead of asking users to assemble those behaviors manually from commands or APIs.
