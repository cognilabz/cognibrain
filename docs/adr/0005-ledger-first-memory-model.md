# Ledger-First Memory Model

Cognibrain will treat the Memory Ledger as the source of truth and derive the Current Memory View from that ledger. This keeps every remembered claim explainable by its observations, corrections, judgments, outcomes, and evidence instead of relying on mutable memory rows whose origin and authority become unclear over time.
