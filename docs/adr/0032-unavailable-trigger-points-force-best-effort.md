# Unavailable Trigger Points Force Best-Effort

If a host tool cannot expose a required Harness Trigger Point, its adapter must be treated as a Best-Effort Harness even when every available trigger works correctly. Partial lifecycle coverage is useful, but it cannot honestly support the full Brain Lifecycle or Mistake Prevention Claim.
