# Repo-Local Signal Noise Threshold

The Signal Noise Threshold is a repo-local single-developer Operator setting. The default remains at least three ignored or judged irrelevant Signal Outcomes spanning at least two agents or sessions; the Operator may override that threshold per repo only with an explicit reason. This keeps Dream tunable for one developer's repo without turning warning weakening into an invisible global behavior.
