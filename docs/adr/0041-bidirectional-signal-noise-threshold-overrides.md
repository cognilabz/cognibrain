# Bidirectional Signal Noise Threshold Overrides

Signal Noise Threshold Overrides may raise or lower the repo-local threshold. Raising the threshold requires more evidence before Dream proposes weakening a warning; lowering the threshold makes warning weakening easier and must be visibly marked that way, with the Operator's explicit reason. Both directions remain repo-local and only affect Dream proposals, not automatic memory changes.
