# Block Overrides Are One-Time by Default

Operator Block Overrides are one-time by default. Persistent bypasses require a separate explicit Operator decision with a reason, because a single unblock should let the current action proceed without quietly becoming a durable policy change that weakens future protection.
