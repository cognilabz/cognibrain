# Block Overrides Require Reasons

Every Operator Block Override requires a short reason, including one-time overrides. This adds a small amount of friction at the moment protection is bypassed, but it gives Dream usable evidence about whether a block was wrong, too broad, or intentionally overridden for a specific case.
