# Evidence-Grade Memory

Evidence-grade memory means a context item is not just retrieved. It is scoped, cited, policy-checked, temporally evaluated and explainable.

EvidencePacks show why memory was included, why other memory was excluded, which graph paths contributed, which policy decisions applied and whether a result was stale or unsafe.

Claim ID: `CB-CLAIM-EVIDENCE`.
