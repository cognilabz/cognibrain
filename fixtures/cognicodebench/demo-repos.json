{
  "schemaVersion": "1.0",
  "demos": [
    {
      "id": "typescript-api",
      "repoType": "TypeScript API",
      "beforeTask": "Add request validation to the user endpoint.",
      "wrongAction": "Edited src/generated/api.generated.ts and ran pnpm test.",
      "correction": "Do not edit generated files; validation belongs in src/validation and this repo uses npm test.",
      "nextTask": "Add validation for organization invites.",
      "expectedNextAction": "Edit src/validation and run npm test.",
      "claimIds": ["CB-CLAIM-CONTEXT", "CB-CLAIM-GUARD"]
    },
    {
      "id": "react-app",
      "repoType": "React app",
      "beforeTask": "Add loading state to the settings panel.",
      "wrongAction": "Changed generated route types directly.",
      "correction": "Generated route types are off limits; update the source component and regenerate.",
      "nextTask": "Add empty state to billing settings.",
      "expectedNextAction": "Edit source component only and avoid generated route types.",
      "claimIds": ["CB-CLAIM-GUARD"]
    },
    {
      "id": "python-fastapi",
      "repoType": "Python FastAPI service",
      "beforeTask": "Add a health route.",
      "wrongAction": "Ran npm test in a Python service.",
      "correction": "Use pytest for this FastAPI service and keep routers under app/routes.",
      "nextTask": "Add readiness route.",
      "expectedNextAction": "Edit app/routes and run pytest.",
      "claimIds": ["CB-CLAIM-CONTEXT"]
    },
    {
      "id": "monorepo",
      "repoType": "Monorepo",
      "beforeTask": "Update the payments package.",
      "wrongAction": "Changed the root lockfile and skipped the package test.",
      "correction": "Use the package-local test command and do not touch the root lockfile for package-only changes.",
      "nextTask": "Update payments refund validation.",
      "expectedNextAction": "Run the package-local test and avoid the root lockfile.",
      "claimIds": ["CB-CLAIM-PATCH-EVIDENCE"]
    },
    {
      "id": "legacy-app",
      "repoType": "Legacy app",
      "beforeTask": "Patch account export.",
      "wrongAction": "Used the new service path that is not active on the legacy branch.",
      "correction": "On legacy branch, exports still live under lib/exports until the migration is complete.",
      "nextTask": "Patch invoice export.",
      "expectedNextAction": "Use lib/exports and mark the stale migration rule as excluded.",
      "claimIds": ["CB-CLAIM-EVIDENCE"]
    }
  ]
}
