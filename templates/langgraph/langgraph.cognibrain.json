{
  "schemaVersion": "1.0",
  "name": "cognibrain-langgraph",
  "runtime": {
    "apiUrl": "http://localhost:8787",
    "startCommand": "node __COGNIBRAIN_ROOT__/bin/cognibrain.mjs start",
    "healthCommand": "node __COGNIBRAIN_ROOT__/bin/cognibrain.mjs doctor --publish"
  },
  "integration": {
    "beforeNode": "fetch a cognibrain coding context pack for the graph state",
    "beforeTool": "check the planned tool action with /code/action-guard",
    "afterTool": "send tool outcome telemetry through /connectors/telemetry",
    "afterPatch": "write a patch evidence trail through /patch-evidence",
    "feedback": "record accepted, rejected, partially useful, or harmful context packs"
  }
}
